# Global properties
#
# The top properties below are applied to every block, but can be overridden.
# Each block command defaults to the script name to avoid boilerplate.
command=/usr/share/i3blocks/$BLOCK_NAME
separator_block_width=15
markup=pango

# crypto
[crypto]
label=
markup=pango
interval=300
instance=bitcoin
command=~/.config/i3blocks-crypto/crypto EUR

[crypto]
markup=pango
interval=300
instance=monero
command=~/.config/i3blocks-crypto/crypto EUR

[crypto]
label=
markup=pango
interval=300
instance=solana
command=~/.config/i3blocks-crypto/crypto EUR


[crypto]
label=
markup=pango
interval=300
instance=ethereum
command=~/.config/i3blocks-crypto/crypto EUR


